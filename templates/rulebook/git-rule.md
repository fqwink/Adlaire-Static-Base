# Git運用ルールテンプレート

保護対象branchを定義する。

保護対象branchへの直接pushを禁止するかどうかを明記する。

変更作業は作業branchで行う。

Pull Request 経由で保護対象branchへ反映する。

Pull Request の merge 権限を明確にする。

remote branch の削除方針を明記する。

local branch の削除条件を明記する。

merge済みを確認できないbranchを削除してはならない。

GitHub repository 設定を管理対象にする場合は、設定項目、変更承認、禁止事項を明記する。
